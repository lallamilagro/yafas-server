sudo: false
language: python
cache: pip

python:
  - "3.6"

install:
  - pip install coveralls -r requirements.txt

script:
  - flake8
  - cp .env.ci .env
  - coverage run --source yafas -m pytest

after_success:
  - coveralls
